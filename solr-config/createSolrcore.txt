// Si no tienen configurado sus variables de entorno, deben estar en la carpeta de los archiivos de Solr

/* Crear core con lo archivos de configuración*/ 
bin/solr create_core -c MiNucleo -d path/to/tu/directorio/conf


/*Iniciar solr*/
bin/solr start


/*Parar solr*/
solr stop -all


/*Cambiar puerto de solr en el proyecto*/
storage.js linea 8


/*Ubicación del core*/
En la carpeta de los archivos de solr
    | server
        | solr 
            | <nombre del core>
                | conf <aquí están los archivos de configuración managed-schema, solrconfig>